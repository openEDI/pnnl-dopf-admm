{
  "description": "Validated static configuration written by /configure and read by the federate.",
  "properties": {
    "name": {
      "description": "Unique identifier for the federate name",
      "title": "Name",
      "type": "string"
    },
    "source_bus": {
      "default": "",
      "description": "ID of the boundary/source bus",
      "title": "Source Bus",
      "type": "string"
    },
    "source_line": {
      "default": "",
      "description": "ID of the boundary/source line",
      "title": "Source Line",
      "type": "string"
    },
    "switches": {
      "description": "List of switches/controllable lines in the area",
      "items": {
        "type": "string"
      },
      "title": "Switches",
      "type": "array"
    },
    "vup_tol": {
      "default": 0.001,
      "description": "Convergence tolerance for upstream voltage mismatch",
      "title": "Vup Tol",
      "type": "number"
    },
    "sdn_tol": {
      "default": 0.001,
      "description": "Convergence tolerance for power mismatch",
      "title": "Sdn Tol",
      "type": "number"
    },
    "rho_vup": {
      "default": 1000.0,
      "description": "ADMM penalty parameter for upstream voltage",
      "title": "Rho Vup",
      "type": "number"
    },
    "rho_sup": {
      "default": 0.0,
      "description": "ADMM penalty parameter for active power injection",
      "title": "Rho Sup",
      "type": "number"
    },
    "rho_vdn": {
      "default": 0.0,
      "description": "ADMM penalty parameter for downstream voltage discrepancy",
      "title": "Rho Vdn",
      "type": "number"
    },
    "rho_sdn": {
      "default": 1000.0,
      "description": "ADMM penalty parameter for active/reactive power flow discrepancy",
      "title": "Rho Sdn",
      "type": "number"
    },
    "max_itr": {
      "default": 100,
      "description": "Maximum number of ADMM iterations per step",
      "title": "Max Itr",
      "type": "integer"
    },
    "relaxed": {
      "default": false,
      "description": "Boolean flag to enable relaxed model formulation",
      "title": "Relaxed",
      "type": "boolean"
    },
    "control_type": {
      "default": "real",
      "description": "Control mode (e.g. 'real', 'reactive')",
      "title": "Control Type",
      "type": "string"
    },
    "number_of_timesteps": {
      "default": 1,
      "description": "Total number of simulation timesteps",
      "title": "Number Of Timesteps",
      "type": "integer"
    },
    "deltat": {
      "default": 3600.0,
      "description": "Co-simulation time step interval in seconds",
      "title": "Deltat",
      "type": "number"
    },
    "objective": {
      "default": "cp_obj_none",
      "description": "OPF objective function name",
      "title": "Objective",
      "type": "string"
    }
  },
  "required": [
    "name"
  ],
  "title": "StaticInputs",
  "type": "object"
}
